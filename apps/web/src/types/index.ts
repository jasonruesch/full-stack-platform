/**
 * The web client speaks the same domain vocabulary as the API. Re-export the
 * shared models and schemas so app code imports from one place (`~/types`).
 */
export * from '@bookmarkvault/shared';
